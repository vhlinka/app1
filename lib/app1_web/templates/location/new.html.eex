<span class="back"><%= link "< Back", to: location_path(@conn, :index) %></span>

<h2 class="form">New Location</h2>

<%= render "form.html", Map.put(assigns, :action, location_path(@conn, :create)) %>
